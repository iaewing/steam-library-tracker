export default function Home({ title = 'Steam Library Tracker' }) {
    return (
        <main style={{ padding: '2rem', fontFamily: 'sans-serif' }}>
            <h1>{title}</h1>
            <p>NativePHP + Inertia + React setup complete.</p>
        </main>
    );
}
